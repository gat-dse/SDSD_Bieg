\chapter{Methods}\label{sec:MM}
\markright{\thechapter~Materials and methods}

This chapter explains the methodology used to assess the sustainability impact of the compared slab systems for residential and office buildings. First, the slab configurator and the material, cost and construction-time data are introduced. Second, the extensions implemented within this thesis are presented, followed by the verification of the implemented models. Third, the comparison procedure is outlined, including the construction of the comparison plots.

\section{Slab configurator}
The computational work presented in this thesis is based on a Python-based design and optimisation framework initiated by the Sustainability in Structural Design Team at kfmResearch \cite{kfmResearch2026}. The original framework provided the general structure for the GWP optimisation of floor system cross-sections across different structural systems and for their environmental performance assessment. Within the scope of this thesis, the framework was further developed by incorporating additional slab systems and implementing the automated handling of non-structural floor build-ups.

Figure~\ref{fig:slab_configurator_workflow} provides a schematic overview of the overall workflow, while Figure~\ref{fig:software_architecture} documents the underlying software architecture using a UML diagram.
The calculation starts with the material and product database. Based on the selected slab system, a cross-section object is created using the available materials and initial geometric parameters. The cross-section is then combined with a member object that defines the structural system, span, loading and design requirements. The member can be verified with respect to ULS, SLS~1, SLS~2 and fire, as defined in Table~\ref{Tab: ReqSS}. In addition, a floor build-up is generated automatically to satisfy the acoustic requirements for airborne and impact sound insulation.\\

\begin{figure}[h]
\centering
 \resizebox{\textwidth}{!}{%
        \input{IMAGES/tikz/Slab_configurator_workflow}
    }
\caption{Slab configurator workflow from material database to cross-section optimisation and member comparison.}
\label{fig:slab_configurator_workflow}
\end{figure}

\newpage


The optimiser modifies selected cross-section parameters to minimise the GWP. The optimisation can be performed either for a single design state or for the combined envelope criterion. In the combined envelope criterion, all considered requirements, ULS, SLS~1, SLS~2, and fire, are included. When a single criterion is selected, the cross-section is optimised subject to the corresponding requirement, allowing its individual influence on the resulting design to be examined. Only solutions satisfying the chosen requirement are considered valid, preventing low-GWP cross-sections that are not feasible.

It is noted that the slab configurator is used as a comparative design tool. It does not replace a detailed project-specific structural design, but it enables a consistent assessment of the relative influence of slab system, span, material choice, acoustic build-up and governing design criterion.


\subsection{Material database}
The material database was selected by the Sustainability in Structural Design Team of \textit{kfmResearch} \cite{kfmResearch2026}. It contains mechanical material properties and product-specific environmental data. Environmental Product Declarations (EPDs) are stored on product level and are linked to the mechanical property classes used by the structural models. The database therefore allows a distinction between mechanical performance and producer-specific environmental impact. For concrete, reinforcing steel, prestressing steel, and timber products, the database stores product density, material class and total GWP values. These properties enable the assessment of producer-specific GWP, mass, and structural height, while accounting for product-specific variations. As identified in the literature review, these parameters are key indicators for the sustainability assessment of floor systems.

The range of product-specific GWP values used in the database is summarised in Table~\ref{tab:product_specific_gwp_values}. Only EPDs within the 0.05--0.95 quantile range were selected to exclude outliers. Each product is assigned to a material class, which defines the corresponding mechanical and physical properties listed in Table~\ref{tab:material_properties}.



\begin{table*}[h]
    \centering
    \begingroup
    \scriptsize
    \caption{Product-specific GWP values used for the sustainability assessment.}
    \label{tab:product_specific_gwp_values}
    \setlength{\tabcolsep}{3pt}
    \renewcommand{\arraystretch}{1.12}
    \begin{tabularx}{\textwidth}{@{}
        >{\raggedright\arraybackslash}X
        |>{\raggedright\arraybackslash}X
        |c|r|r@{}}
    \hline
    \textbf{Material} &
    \textbf{Material class} &
    \textbf{Products} &
    \textbf{$GWP_{\min}$} &
    \textbf{$GWP_{\max}$} \\
    &
    &
    &
    kg CO$_2$-eq/t &
    kg CO$_2$-eq/t \\
    \hline

    Ready-mixed concrete & C20/25 & 1  & 77.0   & 77.0 \\
    Ready-mixed concrete & C25/30 & 3  & 78.7   & 87.3 \\
    Ready-mixed concrete & C30/37 & 1  & 87.2   & 87.2 \\
    Reinforcing steel    & B500B  & 7 & 460.2  & 618.2 \\
    Prestressing steel   & Y1860  & 3  & 588.0  & 1309.0 \\
    Glue-laminated timber& GL24h  & 8 & 116.6 & 319.9 \\
    Solid structural timber & C24 & 1 & 137.1   & 137.1 \\
    3- and 5-ply wood    & C24    & 5 & 170.3  & 418.1 \\
    \hline
    \end{tabularx}
    \endgroup
    
\end{table*}



\begin{table*}[h]
    \centering
    \begingroup
    \scriptsize
    \caption{Mechanical properties data on characteristic design level. Mass is stored producer-specific.}
    \label{tab:material_properties}
    \setlength{\tabcolsep}{3pt}
    \renewcommand{\arraystretch}{1.15}
    \begin{tabularx}{\textwidth}{@{}>{\raggedright\arraybackslash}X|r|r|r|r|r|r|r|r|>{\raggedright\arraybackslash}X|r|r@{}}
  \hline
    \textbf{Class} &
    \textbf{$f_c$} &
    \textbf{$f_t$} &
    \textbf{$f_m$} &
    \textbf{$f_v$} &
    \textbf{$E$} &
    \textbf{$\rho$} &
    \textbf{$\beta_n$} &
    \textbf{$\varphi$} &
    \textbf{Connector type} &
    \textbf{$d$} &
    \textbf{$K_{\mathrm{ser}}$} \\
    &
    MPa &
    MPa &
    MPa &
    MPa &
    GPa &
    kg/m$^3$ &
    mm/min &
    -- &
    -- &
    mm &
    N/m \\
    \hline

    C20/25 &
    20 &
    2.3 &
    -- &
    -- &
    30 &
    2440 &
    -- &
    2.5 &
    -- &
    -- &
    -- \\

    C25/30 &
    25 &
    2.6 &
    -- &
    -- &
    32 &
    2400--2439 &
    -- &
    2.5 &
    -- &
    -- &
    -- \\

    C30/37 &
    30 &
    2.9 &
    -- &
    -- &
    34 &
    2347 &
    -- &
    2.5 &
    -- &
    -- &
    -- \\

    B500B &
    500 &
    500 &
    -- &
    -- &
    205 &
    7850 &
    -- &
    -- &
    -- &
    -- &
    -- \\

    GL24h &
    -- &
    -- &
    24 &
    1.8 &
    11.5 &
    424--480 &
    0.7 &
    0.6 &
    -- &
    -- &
    -- \\

    C24 &
    -- &
    -- &
    24 &
    1.5 &
    11.0 &
    470 &
    0.8 &
    0.6 &
    -- &
    -- &
    -- \\

    Y1860 &
    -- &
    1860 &
    -- &
    -- &
    195 &
    7850 &
    -- &
    -- &
    -- &
    -- &
    -- \\

    DBS\_10 &
    -- &
    -- &
    -- &
    -- &
    -- &
    -- &
    -- &
    -- &
    Screw &
    10 &
    $9.70 \times 10^6$ \\

    Kerve\_20 &
    -- &
    -- &
    -- &
    -- &
    -- &
    -- &
    -- &
    -- &
    Notched &
    -- &
    $1.00 \times 10^9$ \\

    Kerve\_30 &
    -- &
    -- &
    -- &
    -- &
    -- &
    -- &
    -- &
    -- &
    Notched &
    -- &
    $1.50 \times 10^9$ \\

    Two-layer parquet &
    -- &
    -- &
    -- &
    -- &
    -- &
    555 &
    -- &
    -- &
    -- &
    -- &
    -- \\

    Cement screed &
    -- &
    -- &
    -- &
    -- &
    21.0 &
    1850 &
    -- &
    -- &
    -- &
    -- &
    -- \\

    Insulation (glass wool) &
    -- &
    -- &
    -- &
    -- &
    $6\times10^{-6}$ &
    80 &
    -- &
    -- &
    -- &
    -- &
    -- \\
    
    Crushed gravel &
    -- &
    -- &
    -- &
    -- &
    -- &
    2000 &
    -- &
    -- &
    -- &
    -- &
    -- \\

    \hline
    \end{tabularx}
    \endgroup
    
\end{table*}

\newpage
During the thesis, the database was extended with material-specific (not producer-specific) cost and construction time as listed in Table~\ref{tab:cost_time_assumptions}. The values are derived from NPK positions, market data, literature sources and engineering estimates. The assumptions are modelled with an accuracy range of \(\pm 20\,\%\) using triangular distributions. This approach reflects common building practice, where early design explorations are typically carried \mbox{out with a cost accuracy of \(\pm 20\,\%\) \cite{MenzReichel2018_Kosten}.}

\begin{table*}[h]
    \centering
    \begingroup
    \scriptsize
    \caption{Cost and construction-time input data used for the sustainability assessment.}
    \label{tab:cost_time_assumptions}
    \setlength{\tabcolsep}{3pt}
    \renewcommand{\arraystretch}{1.12}
    \begin{tabularx}{\textwidth}{@{}X|c|c|c|c|X@{}}
    \hline
    \textbf{Material / Component} &
    \textbf{Cost} &
    \textbf{Cost unit} &
    \textbf{Time} &
    \textbf{Time unit} &
    \textbf{Reference and description} \\ \hline

    Ready-mixed concrete, formwork &
    75.71 & CHF/m$^2$ & 0.70 & h/m$^2$ &
    NPK 241.261.301, logistics, propping, levelling, oiling and cleaning \cite{CIB19076}.\\

    Ready-mixed concrete, rib. formwork &
    151.42 & CHF/m$^2$ & 1.40 & h/m$^2$ &
    Logistics, propping, levelling, oiling and cleaning \cite{Ammann2024}.\\

    Reinforcing steel &
    2.01 & CHF/kg & 0.009 & h/kg &
    NPK 241.511.123, placing \cite{CIB19076}.\\

    Prestressing steel &
    6.00 & CHF/kg & 0.020 & h/kg &
    Assumed cost and labour input. \\

    Ready-mixed concrete &
    205.29 & CHF/m$^3$ & 0.30 & h/m$^3$ &
    NPK 241.661.112, placing, vibrating, levelling and cleaning \cite{CIB19076}. \\

    Glue-laminated timber &
    1750.00 & CHF/m$^3$ & 1.00 & h/m$^3$ &
    Assumed market price including logistics and connections. \\

    Solid structural timber &
    1250.00 & CHF/m$^3$ & 1.00 & h/m$^3$ &
    NPK 335.531.114, logistics and connections. \\

    DBS 6, Screw TCC conn., d=6 mm &
    5.00 & CHF/piece & 0.010 & h/piece &
    Assumed, logistics and assembly. \\

    DBS 8, Screw TCC conn., d=8 mm &
    5.50 & CHF/piece & 0.010 & h/piece &
    Assumed, logistics and assembly. \\

    DBS 10, Screw TCC conn., d=10 mm &
    6.00 & CHF/piece & 0.015 & h/piece &
    Assumed, logistics and assembly. \\

    DBS 12, Screw TCC conn., d=12 mm &
    6.50 & CHF/piece & 0.015 & h/piece &
    Assumed, logistics and assembly. \\

    Kerve, TCC connector &
    30.00 & CHF/piece & 0.010 & h/piece &
    Assumed, preparation and cleaning. \\

    Two-layer parquet, 11 mm &
    106.00 & CHF/m$^2$ & 0.30 & h/m$^2$ &
    Swiss market price according to \cite{Bodenleger2026}. \\

    Cement screed &
    337.60 & CHF/m$^3$ & 0.15 & h/m$^2$ &
    NPK 661.711.111, labour productivity according to \cite{Steelmonks2026}. \\

    Insulation (glass wool) &
    335.00 & CHF/m$^3$ & 0.10 & h/m$^2$ &
    NPK 661.433.114, acoustic insulation layer. \\

    Crushed gravel &
    253.20 & CHF/m$^3$ & 0.20 & h/m$^2$ &
    NPK 364.921.111, ballast and protection layer. \\ \hline
    \end{tabularx}
    \endgroup
    
\end{table*}

\subsection{Contribution to the existing Python framework}
Since this thesis is based on an existing Python framework, it is important to highlight the contributions made within its scope. 
The initial configurator was able to handle solid rectangular concrete, ribbed concrete, solid rectangular timber, and ribbed timber hollow-core cross-sections. To distinguish these from the cross-sections added within this thesis, systems already handled by the initial slab configurator are marked with (*) in the legends.

The one-dimensional static systems covered were simple-span and continuous beams. For the two-dimensional case, the framework was able to handle line-supported rectangular slabs without continuity over the supports.

\subsubsection{Cross-section handling}
All cross-section classes were updated with respect to the calculation of material quantities, GWP, cost, and construction time. These calculations are now normalised per square metre of floor area and account for quantities that develop in the transverse direction. This enables a consistent comparison between one-dimensional systems with defined tributary widths and two-dimensional slab systems. Furthermore, all systems were linked to the automated acoustic floor build-up generator described in Section~\ref{Sec: AutFloor}. During the optimisation, each cross-section candidate is combined with the lightest build-up that satisfies the specified acoustic requirements. The optimisation therefore accounts for the additional mass introduced by the non-structural layers. Since these layers consist of a cement screed on an insulation layer, the damping coefficient for the vibration assessment was set to \(\zeta = 0.02\) for all cross-sections \cite{Lignum2021_HBT1}. The specific modifications made to the individual cross-section classes are summarised in Table~\ref{tab:existing_system_developments}.

\begin{table*}[h]
    \centering
    \begingroup
    \scriptsize
    \caption{Cross-section-specific developments introduced for existing structural systems.}
    \label{tab:existing_system_developments}
    \setlength{\tabcolsep}{3pt}
    \renewcommand{\arraystretch}{1.15}
    \begin{tabularx}{\textwidth}{@{}
        >{\raggedright\arraybackslash}p{3.2cm}
        |>{\raggedright\arraybackslash}X
        |>{\raggedright\arraybackslash}X@{}}
    \hline
    \textbf{Cross-section class} &
    \textbf{Further development} &
    \textbf{Effect on the assessment} \\
    \hline

    Ribbed timber hollow-core &
    Internal cavity insulation added to the acoustic model and non-structural mass calculation. &
    Improves the acoustic model of the hollow-core cross-section.\\

    Ribbed reinforced concrete &
    Separate resistance checks introduced for positive span bending and negative support bending.  &
    Extends ULS validation from span moment to support moment.  \\

    Rectangular reinforced concrete &
    Two-way slab analysis extended to include upper support reinforcement in the optimisation for continuous systems. As equal spans are assumed, the optimised reinforcement in the \(x\)-direction is applied in the \(y\)-direction. &
    Enables the optimisation of negative bending reinforcement quantities for continuous systems. \\

    Point-supported reinforced concrete &
    Punching shear calculation of the required punching reinforcement resistance. Required punching reinforcement is converted into an additional steel quantity. &
    Includes the environmental, economic and construction-time effects of punching reinforcement without excluding systems solely based on the need for such reinforcement. \\

    \hline
    \end{tabularx}
    \endgroup
    
\end{table*}

\subsubsection{Static system handling}
The analysis of two-dimensional floor systems was extended to distinguish between line-supported and point-supported structural systems. In addition, continuity over the supports was introduced. Internal forces and deflections were determined in accordance with the existing implementation using:

\begin{equation}
\begin{aligned}
M_d &= \alpha_{M,\mathrm{system}} \ q_d l^2,\\
V_d &= \alpha_{V,\mathrm{system}} \ q_d l,\\
w &= \alpha_{w,\mathrm{system}}\ \frac{q_d l^4}{EI}.
\end{aligned}
\end{equation}

The coefficients $\alpha_{M,\mathrm{system}}$, $\alpha_{V,\mathrm{system}}$ and $\alpha_{w,\mathrm{system}}$ were derived from a finite-element slab analysis performed in Cubus Cedrus. For point-supported systems, the model consisted of a regular $4 \times 4$ column grid subjected to a uniformly distributed load. The columns had a cross-section of $0.25 \times 0.25 \mathrm{m}$. Line-supported systems were represented by walls with a thickness of $0.20\mathrm{m}$ and a height of $0.10\mathrm{m}$. Vertical displacements at the supports were restrained. Bending moments and shear forces were evaluated over an averaged width of one metre within the support strip, whereas deflections were evaluated at mid-span. The coefficients were determined for spans of 3, 5, 6, 7, 8, 10, 12, and 16\,m and subsequently implemented in the configurator through a database storage. 

For the ULS bending verification of two-dimensional rectangular concrete slabs, plastic moment redistribution was introduced. The elastic coefficients from the slab database remain the basis of the analysis, but they are modified if the cross-section provides sufficient rotational capacity. For this purpose, the criterion \(x/d < 0.35\) is checked. If this criterion is not fulfilled, the elastic coefficients are retained. If it is fulfilled, the negative support moments are reduced and the positive span moments are increased using system-specific redistribution factors. For line-supported continuous slabs, the redistribution factors \(k_{+}=1.51\) and \(k_{-}=0.76\) were used. For point-supported continuous slabs, the corresponding factors were \(k_{+}=1.47\) and \(k_{-}=0.68\). These factors were derived by applying an 80\% stiffness reduction over a length of \(0.15\,L_0\) in the support region in the finite-element slab analysis.


\subsection{Timber--concrete composite slab}

Timber--concrete composite (TCC) slabs were modelled as unidirectional structural systems consisting of a timber tension member and a concrete compression layer. Composite action is partially achieved through mechanical shear connectors. 

\subsubsection{$\gamma$-method}

The implemented model for partial shear connection follows the $\gamma$-method according to EN 1995-1-1 \cite{Eurocode5_2023}. The effective bending stiffness is calculated as
\begin{equation}
(EI)_{\mathrm{ef}}
=
\sum_{i=1}^{2}
\left(
E_i I_i
+
\gamma_i E_i A_i a_i^2
\right),
\qquad
\gamma_1
=
\left(
1+
\frac{\pi^2 E_1 A_1 s_1}{K l^2}
\right)^{-1},
\qquad
\gamma_2 = 1.
\end{equation}
Index 1 refers to the concrete layer and index 2 to the timber layer. The factors $\gamma_i$ describe the efficiency of the composite action resulting from the finite slip stiffness of the connection. The distances from the composite neutral axis to the centroids of the concrete and timber components, as well as the connector stiffness, are calculated as
\begin{equation}
\begin{aligned}
a_1
&=
\frac{h_1+h_2}{2}
+d
-a_2,
\\
a_2
&=
\frac{
\gamma_1 E_1 A_1
\left(
\frac{h_1+h_2}{2}
+
d
\right)
}
{
\gamma_1 E_1 A_1
+
\gamma_2 E_2 A_2
},
\\
K
&=
\frac{2}{3}K_{\mathrm{ser}}.
\end{aligned}
\end{equation}
For the concrete compression flange, an effective width is considered according to
\begin{equation}
\begin{aligned}
b_{\mathrm{ef}}
&=
\min
\left(b_{\mathrm{ef},1}
+
b_{\mathrm{ef},2}
+
b_w, a_{ribs}\right),
\\
b_{\mathrm{ef},i}
&=
\min
\left(
0.2\,b_i
+
0.1\,l_0,
\;
0.2\,l_0
\right).
\end{aligned}
\end{equation}
Here, $E_i$, $I_i$ and $A_i$ are the modulus of elasticity, moment of inertia and cross-sectional area of part $i$, respectively. The connector spacing is denoted by $s_1$, while $K$ is the connector slip modulus used in the $\gamma$-method. Furthermore, $l_0$ is the span length, $b_i$ is the width of part $i$, $a_{ribs}$ distance between ribs, $h_i$ is the depth of part $i$, and $d$ is the depth of the formwork or interlayer.

\subsubsection{Time-dependent behaviour}

TCC systems are verified at different time states because stresses redistribute between timber and concrete due to their different time-dependent creep behaviour. For the ULS of the composite cross-section, the quasi-permanent design load and the short-term component are calculated as
\begin{equation}
\begin{aligned}
q_{\mathrm{perm},d}
&=
1.25
\left(
\gamma_G\, g_{k,\mathrm{tot}}
+
\psi_{01}\,\gamma_Q\, q_{k1}
\right),
\\
q_{\mathrm{short},d}
&=
(1-\psi_{01})
\,\gamma_Q\, q_{k1}.
\end{aligned}
\end{equation}
For the SLS, the permanent, frequent and rare load combinations are calculated as
\begin{equation}
\begin{aligned}
q_{\mathrm{perm}}
&=
g_{k,\mathrm{tot}}
+
\psi_{21}\,q_{k1},
\\
q_{\mathrm{freq}}
&=
g_{k,\mathrm{tot}}
+
\psi_{11}\,q_{k1},
\\
q_{\mathrm{rare}}
&=
g_{k,\mathrm{tot}}
+
q_{k1}.
\end{aligned}
\end{equation}
The verification for the intermediate period of 3--7 years, which accounts for differential shrinkage, is not required as the ULS check is verified for 125\% of the quasi-permanent load \cite{CENTS191032021}. The calculations consider the concrete creep coefficient $\varphi$, the timber deformation factor $k_{\mathrm{def}}$ and the effective connector deformation factor
\begin{equation}
k'_{\mathrm{def}}
=
2k_{\mathrm{def}}.
\end{equation}
Consequently, ULS verification is separated into short-term and quasi-permanent loading situations, each using different effective stiffnesses. SLS verifications also account for long-term effects. For deflection calculations, a distinction is made between short-term and quasi-permanent loading, whereas vibration calculations are based on the stiffness at $t=0$ and assume an uncracked concrete section in the transverse direction. In accordance with CEN/TS 19103 \cite{CENTS191032021}, the effective bending stiffnesses $EI_{\mathrm{ULS},t=0}$, $EI_{\mathrm{ULS},t=\infty}$, $EI_{\mathrm{SLS},t=0}$ and $EI_{\mathrm{SLS},t=\infty}$ were calculated accordingly.

\subsubsection{Resistance verification}

The moment and shear resistances are determined per metre width of slab. The governing bending resistance is obtained from the minimum reserve of the concrete compression zone and the timber bending resistance. Figure \ref{fig:composite_section} illustrates the stress distribution for a composite beam with semi-rigid connection for the verification.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{IMAGES/CS_sigmas.png}
    \caption{Cross-section of a two-layer composite beam with semi-rigid connection according to EN 1995-1-1 \cite{Eurocode5_2023}.}
    \label{fig:composite_section}
\end{figure} \vspace{-0.8cm}

The bending and shear resistances are calculated as
\begin{equation}
\begin{aligned}
m_u
&=
\frac{1}{b}
\min
\left(
f_{cd,1}
\frac{(EI)_{\mathrm{ef}}}{E_1}
\frac{1}{\gamma_1 a_1 + h_1/2},
\;
f_{md,2}
k_{\mathrm{mod}}
\frac{(EI)_{\mathrm{ef}}}{E_2}
\frac{1}{a_2 + h_2/2}
\right),
\\
v_u
&=
\frac{
2 f_{vd,2} k_{\mathrm{mod}}
}
{b}
\frac{(EI)_{\mathrm{ef}}}{E_2}
\frac{1}
     {(a_2+h_2/2)^2}.
\end{aligned}
\end{equation}
The shear resistance is governed by the timber member. For simplification, the shear connection was assumed to be sufficient. Consequently, the detailed design of the connectors was not considered and no connector resistance verification was performed.

\subsubsection{Vibration verification}
The vibration verification was carried out according to Lignum HBT1 \cite{Lignum2021_HBT1}. In the longitudinal direction, the short-term serviceability bending stiffness \(EI_{\mathrm{SLS},t=0}\) was used. In the orthogonal direction, the uncracked concrete bending stiffness was assumed.

\subsubsection{Fire verification}
Fire resistance was evaluated using the residual timber cross-section after charring. A distinction was made between beams exposed on three sides and timber plates exposed on one side. For notched connections, no temperature-dependent reduction in connector stiffness was considered. For screw connectors, the reduction factor $k_{\mathrm{mod,fi}}$ according to Lignum 3.1 was applied \cite{Lignum3.1:2015}.

To satisfy the REI60 requirement, a minimum concrete topping thickness of 80 mm was required \cite{SIA262_2025}. Furthermore, a timber cover of 30 mm was reserved for screw connectors to account for the arrangement of two screws placed side by side.

\subsubsection{Model limitations}

Several simplifications were adopted. The model is limited to one-way structural behaviour and linear-elastic composite action according to the $\gamma$-method. Concrete is assumed to remain uncracked. The shear connection was assumed to provide sufficient resistance. Furthermore, non-linear connector behaviour and differential shrinkage between timber and concrete were not considered.

\subsection{Post-tensioned concrete slab}

Post-tensioned concrete slabs were modelled as flat concrete slabs with unbonded monostrands \cite{Rombach2010, flachdecken_Morgen}. The implemented system assumes parabolic tendon profiles. For one-dimensional systems, a distributed tendon layout was assumed. For two-dimensional systems, the tendon layout was represented either by a distributed or a banded arrangement, as illustrated in Figure~\ref{fig:distributed_banded_tendon_layouts}. Minimum slab thickness and spacing requirements were defined in accordance with the technical documentation of VSL Switzerland \cite{VSL_STA01007}.

\subsubsection{Load balancing and tendon geometry}

The post-tensioning force is calibrated from the load-balancing principle to compensate $100\%$ structural self-weight.  The upward deviation force depends on the prestressing force, tendon eccentricity and span length, given the parabolic tendon trajectory. For distributed tendons, the equivalent deviation loads in the two orthogonal directions are calculated as

\begin{equation}
u_x
=
\frac{8p_x f_x}{l_x^2},
\qquad
u_y
=
\frac{8p_y f_y}{l_y^2}.
\end{equation}

Here, \(p_x\) and \(p_y\) are the distributed prestressing forces per metre width, \(f_x\) and \(f_y\) are the tendon sag values, and \(l_x\) and \(l_y\) are the corresponding spans. The implemented load-balancing algorithm assigns the required balanced load depending on the selected tendon layout. For distributed tendons in both directions, the structural self-weight is split equally between the two directions and represented by equivalent surface deviation loads. For banded layouts, the tendons are concentrated in the support strips and the internal forces caused by prestressing are derived from equivalent line loads acting along these strips. 

The tendon profile was defined by the eccentricities at the supports and at mid-span. The eccentricities were selected as the maximum values that satisfy the tendon cover requirements, avoid overlap with the bonded reinforcement, and remain within the slab depth.

\begin{equation}
z(x)
=
-\frac{4(e_{\mathrm{span}}-e_{\mathrm{sup}})}{l^2}\,x^2
+
\frac{4(e_{\mathrm{span}}-e_{\mathrm{sup}})}{l}\,x
+
e_{\mathrm{sup}}.
\end{equation}


\subsubsection{Secondary internal forces}

In hyperstatic systems, the deformation due to post-tensioning deviation forces is restrained by the supports. This restraint introduces secondary bending moments and secondary shear forces. The total internal forces resulting from the deviation forces of the prestressing system are first calculated using the slab-system coefficients. The secondary internal forces are then obtained by subtracting the primary prestressing contribution:

\begin{equation}
M_{\mathrm{sec}}
=
M_{\mathrm{tot}}(u)
-
P\,e,
\qquad
V_{\mathrm{sec}}
=
V_{\mathrm{tot}}(u)
-
P\,\frac{\partial e}{\partial x}.
\end{equation}

Here, \(M_{\mathrm{tot}}\) and \(V_{\mathrm{tot}}\) denote the total internal forces of the hyperstatic system caused by the deviation forces, \(P\) is the prestressing force and \(e\) is the tendon eccentricity. These secondary internal forces are included in the resistance verification of the post-tensioned slab.

\subsubsection{Effective bending stiffness}

Post-tensioning introduces compressive stresses into the concrete. Thus, the member stays longer in the uncracked state under service loads. For SLS verification, the effective bending stiffness is determined based on the relation between the acting service moment, including secondary moments, and the cracking moment. The cracking moment, initial concrete stress and effective tensile strength are calculated as

\begin{equation}
\begin{aligned}
M_R
&=
\left(
f_{\mathrm{ctm,eff}}
-
\sigma_{c,\mathrm{inf},0}
\right)
\frac{I_I}{h/2},\\
\sigma_{c,\mathrm{inf},0}
&=
-\frac{P}{l\,h}
-
\frac{P\,e_{\max}\,h}{2l\,I_I},\\
f_{\mathrm{ctm,eff}}
&=
\max
\left[
(1.6-h)\,f_{\mathrm{ctm}},
\;
f_{\mathrm{ctm}}
\right].
\end{aligned}
\end{equation}

If the acting service moment does not exceed the cracking moment, the uncracked stiffness is used:
\begin{equation}
(EI)_{\mathrm{eff}}
=
E_{cm}I_I
\qquad
\text{for}
\qquad
\left|M_{\mathrm{Ed,SLS}}+M_{\mathrm{sec}}\right|
\leq
M_R.
\end{equation}

If the cracking moment is exceeded, a partial-cracking factor is calculated and the effective stiffness is interpolated between the uncracked and cracked states according to EN 1992-1-1 \cite{DIN_EN_1992_1_1_2025}:
\begin{equation}
\begin{gathered}
\zeta=
1
-
0.5
\left(
\frac{M_R}{|M_{\mathrm{Ed,SLS}}+M_{\mathrm{sec}}|}
\right)^2,\\[0.5em]
(EI)_{\mathrm{eff}}
=
\zeta (EI)_I
+
(1-\zeta)(EI)_{II}.
\end{gathered}
\end{equation}

For cracked sections, the local increase of the post-tensioning force is neglected because strain compatibility between concrete and unbonded tendons is not maintained. The cracked neutral-axis depth \(x_{II}\) is obtained from force and moment equilibrium using the effective prestressing force as an external normal force averaged over the slab width:
\begin{equation}
\begin{gathered}
A_s\sigma_s + \frac{P}{l}
=
\frac{b\,x_{II}}{2}\sigma_{c,\mathrm{inf}},
\\[0.5em]
d_sA_s\sigma_s
+
d_P\frac{P}{l}
+
M_{\mathrm{Ed,SLS}}
+
M_{\mathrm{sec}}
=
\frac{b\,x_{II}^2}{6}\sigma_{c,\mathrm{inf}},
\\[0.5em]
\sigma_s
=
\frac{\varepsilon_{c,\mathrm{inf}}}{x_{II}}
(d_s-x_{II})E_s,
\qquad
\sigma_{c,\mathrm{inf}}
=
E_{cm}\varepsilon_{c,\mathrm{inf}}.
\end{gathered}
\end{equation}

The cracked bending stiffness is calculated from the concrete compression zone and the transformed bonded reinforcement contribution:
\begin{equation}
(EI)_{II}
=
E_{cm}
\left[
\frac{x_{II}^3}{3}
+
\frac{E_s}{E_{cm}}
A_s
(d_s-x_{II})^2
\right].
\end{equation}

The prestressing tendon is not added as a transformed reinforcement area in the cracked inertia because the tendon is unbonded and local strain compatibility is not assumed. Creep is considered in the member deflection calculation through the service load combinations and creep amplification, rather than by reducing \(E_{cm}\) directly in the cracked-section stiffness calculation in accordance with the existing rectangular concrete handling.

\subsubsection{Bending and shear resistance}

The bending resistance accounts for the contribution of the prestressing steel and the bonded reinforcement. The design bending moment includes the effects of external loads and secondary moments:
\begin{equation}
M_d
=
M_{g,d}
+
M_{q,d}
+
M_{\mathrm{sec}}
\leq
M_{Rd}.
\end{equation}

The bending resistance and the corresponding compression-zone depth are calculated as
\begin{equation}
\begin{gathered}
M_{Rd}
=
\frac{P}{l}
\left(
d_P-\frac{0.85x}{2}
\right)
+
A_s f_{sd}
\left(
d_s-\frac{0.85x}{2}
\right),
\\[0.5em]
x
=
\frac{
A_s f_{sd}
+
P/l
}
{
b_{\mathrm{eff}} f_{cd}
}.
\end{gathered}
\end{equation}

Ductility is ensured by limiting the compression-zone depth and by providing minimum bonded reinforcement. For the minimum bonded reinforcement check, the ordinary reinforced-concrete cracking moment is used.

For systems without column supports, one-way shear is checked. The design shear force includes the secondary shear force and is verified against the concrete shear resistance and the vertical component of the prestressing force:
\begin{equation}
\begin{gathered}
V_d
=
V_{g,d}
+
V_{q,d}
+
V_{\mathrm{sec}}
\leq
V_{Rd}
+
P_{\infty}\sin(\beta_P),\\[0.5em]
\sin(\beta_P)
=
\frac{4f}{L}.
\end{gathered}
\end{equation}

Here, \(V_{Rd}\) is calculated according to the reinforced concrete shear resistance model of the rectangular concrete section. The post-tensioning contribution is added through the vertical component of the prestressing force.

\newpage
\subsubsection{Punching shear resistance}

For slabs supported by columns, punching shear is checked instead of one-way shear. The concrete punching resistance without punching reinforcement is implemented as level of approximation 2\cite{SIA262_2025}
\begin{equation}
\begin{gathered}
V_{Rd,c}(\psi)
=
k_r\,\tau_{cd}\,d_v\,u,
\\ [0.5em]
k_r
=
\frac{1}
{
0.45
+
0.18\,\psi\,d_v\,k_g
}
\leq
2,
\qquad
k_g
=
\frac{48}
{16+D_{\max}}.
\end{gathered}
\end{equation}

The slab rotation is calculated as
\begin{equation}
\begin{aligned}
\psi
&=
1.5
\frac{r_s}{d_v}
\frac{f_{sd}}{E_s}
\left(
\frac{m_{sd}}{m_{Rd}}
\right)^{3/2},\\
r_s &= 0.22l.
\end{aligned}
\end{equation}


For post-tensioned slabs, the resistance is increased by the vertical component of the prestressing force above the column. The prestressing contribution is calculated from the support-strip tendons and the distributed tendons crossing the control width around the column:
\begin{equation}
V_{P,\mathrm{punch}}
=
\left(
P_{sx,\infty}
+
p_{dx,\infty} b_{0,y}
\right)
\sin(\beta_{P,x})
+
\left(
P_{sy,\infty}
+
p_{dy,\infty} b_{0,x}
\right)
\sin(\beta_{P,y}).
\end{equation}

The forces \(P_{sx}\) and \(P_{sy}\) are the post-tensioning forces in the support strips and are assumed to act fully over the column. Distributed tendons are only considered over the relevant control width around the column.

Punching shear is not included in the ULS verification within the optimisation of point-supported slabs. Instead, only bending is checked. This simplification was adopted because practical punching shear detailing, such as mushroom heads, steel shear heads, or punching shear reinforcement, cannot be represented without substantial modelling effort. Consequently, no separate punching reinforcement object is introduced in the structural model. For material accounting in the final comparison, the required punching reinforcement is post-processed as an additional steel quantity based on the total punching resistance calculated as

\begin{equation}
V_{Rd,\mathrm{punch}}
=
V_{Rd,c}(\psi)
+
V_{Rd,s}
+
V_{P,\mathrm{punch}}.
\end{equation}

\subsubsection{Vibration verification}

The vibration verification was conservatively carried out according to Lignum HBT1 \cite{Lignum2021_HBT1}. This simplification allows vibration calculations to be handled consistently and is considered sufficient, as the vibration design state is usually not governing. For slab systems, the vibration calculation was extended to two-way spanning systems by introducing factors that account for the static system in the eigenfrequency calculation \cite{Ziegler2008Grundlagen}.

\subsubsection{Fire verification}
The fire verification is carried out according to SIA 262 \cite{SIA262_2025} by satisfying the minimum member dimensions and reinforcement cover requirements for post-tensioned systems.


\subsubsection{Model limitations}
Several simplifications were adopted. The model is limited to unbonded monostrands with parabolic tendon profiles. Friction losses were neglected. Local anchorage verification and local concrete crushing at the anchorages were not considered. The model also assumes that the selected prestressing layout can be arranged within the geometric constraints of the slab and anchorage system. For cracked sections, the local increase in prestressing force is neglected because strain compatibility is not maintained for unbonded tendons. The normal force introduced by the tendons was averaged across the slab width for the bending resistance calculation. The model is limited to ULS bending resistance. Although punching resistance is calculated, it is only considered indirectly through the required punching reinforcement used for the material calculations.

\newpage
\subsection{Automated acoustic floor build-up generator \label{Sec: AutFloor}}

The generator enables floor build-up handling dependent on the structural cross-section. It adds the lightest non-structural floor build-up that satisfies the selected airborne and impact sound targets. The purpose is to compare slab systems on the basis of complete floor assemblies rather than structural cross-sections only. The generator distinguishes between normal and increased acoustic requirements according to SIA 181 \cite{SIA181_2020}. The target values are summarised in Table~\ref{Tab: ReqSS}.

The workflow of the generator is shown in Figure~\ref{fig:acoustic_floor_generator}. First, the structural cross-section is converted into an area-related mass. Airborne sound insulation and impact sound level are then estimated with the mass-law relationship \cite{BauphysikKalender, ISO12354_1_2017, ISO12354_2_2017}:

\begin{equation}
\begin{aligned}
R_{w,\mathrm{mass}} &= 37.5 \log_{10}(m'_{\mathrm{base}}) - 42,\\
L_{n,w,\mathrm{mass}} &= 164 - 35 \log_{10}(m'_{\mathrm{base}}).
\end{aligned}
\end{equation}

If the bare structural cross-section does not fulfil the acoustic targets, a floating floor consisting of glass wool, cement screed and parquet is added. Screed thicknesses of 60 mm and 85 mm are checked first. The floating floor is represented by a simplified mass-spring-mass model \cite{BauphysikKalender, ISO12354_1_2017, ISO12354_2_2017}:

\begin{equation}
\begin{aligned}
(s'_{\mathrm{eff}})^{-1}
&=
\sum_i (s'_i)^{-1},\\
f_{0,R} &=
\frac{1}{2\pi}
\sqrt{
s'_{\mathrm{eff}}\,10^6
\left(
\frac{1}{m'_{\mathrm{base}}}
+
\frac{1}{m'_{\mathrm{screed}}}
\right)
},\\
\Delta R_{w,\mathrm{fl}} &=
74.4 - 20\log_{10}(f_{0,R}) - \frac{R_{w,\mathrm{mass}}}{2},\\
f_{0,L} &=
160\sqrt{\frac{s'_{\mathrm{eff}}}{m'_{\mathrm{screed}}}},\\
\Delta L_{n,w,\mathrm{fl}} &=
\operatorname{mean}_{f_i \leq f_{\max}}
\left[
30\log_{10}
\left(
\frac{f_i}{f_{0,L}}
\right)
\right].
\end{aligned}
\end{equation}

Here, \(s'_{\mathrm{eff}}\) is the effective dynamic stiffness of the glass-wool layer in \(\mathrm{MN/m^3}\). For TCC sections with two insulation layers, the layers are treated as springs in series.

\newpage
If the acoustic targets are still not met, gravel is added below the insulation layer in discrete steps. TCC sections use a separate branch because the concrete topping is already part of the structural cross-section. The concrete layer is therefore treated as both mass and impact-sound damping and is increased instead of adding additional gravel. Gravel, hollow-core insulation, and the TCC concrete topping all use damping corrections calibrated according to literature measurements \cite{BauphysikKalender}.

\begin{figure}[htbp]
\centering
\resizebox{\textwidth}{!}{%
    \input{IMAGES/tikz/AccGenerator.tex}
}
\caption{Workflow of the automated acoustic floor build-up generator.}
\label{fig:acoustic_floor_generator}
\end{figure}

The implemented acoustic model is intentionally simplified. A fully spectral prediction would require measured $R(f)$, $L_n(f)$ and $\Delta L(f)$ data for every structural system and floor layer. Such data is not generally available for all systems in the optimisation. Thus, the model averages across the calculated frequencies. The selected model is therefore used as a robust comparative approximation, not as a replacement for project-specific acoustic verification.


\section{Verification of implemented models}

\subsection{Timber--concrete composite slab}

The implementation of the $\gamma$-method was verified against a hand calculation based on a representative timber--concrete composite floor system. The verification focused on the effective bending stiffness, as this quantity governs both resistance and serviceability calculations.

The verification model used the following set-up:\vspace{-0.8em}
\begin{itemize}
    \item Concrete C30/37: $h_c = 0.080\,\mathrm{m}$, $b_{\mathrm{ef}} = 0.60\,\mathrm{m}$, $A_c = 0.048\,\mathrm{m}^2$, $I_c = 2.56 \times 10^{-5}\,\mathrm{m}^4$, $E_{cm} = 34\,\mathrm{GPa}$, $\varphi_c = 2.5$.\vspace{-0.8em}
    \item Timber ribs GL24h: $a_{\mathrm{ribs}} = 0.6\,\mathrm{m}$, $h_w = 0.241\,\mathrm{m}$, $b_w = 0.12\,\mathrm{m}$, $A_w = 0.0289\,\mathrm{m}^2$, $I_w = 1.399 \times 10^{-4}\,\mathrm{m}^4$, $E_{w,\mathrm{mean}} = 11.5\,\mathrm{GPa}$, $d = 0.02\,\mathrm{m}$, $k_{\mathrm{def}} = 0.6$.\vspace{-0.8em}
    \item Connection skrews: $d=10mm$, $s = 0.25\,\mathrm{m}$, $K_{\mathrm{ser}} = 9.7\,\mathrm{MN/m}$, $k'_{\mathrm{def}} = 1.2$.\vspace{-0.8em}
    \item Structural system: single-span beam with $l_0 = 6.0\,\mathrm{m}$.\vspace{-0.8em}
\end{itemize}


Table~\ref{tab:TCCVer} summarises the calculated $\gamma_c$ factors, the lever arms to the neutral axis ($a_c$ and $a_w$), and the resulting effective bending stiffness per metre width. The comparison showed excellent agreement between the manual calculations and the implementation, confirming the correct implementation of the $\gamma$-method across the time-dependent stiffness calculations.

\begin{table}[h!]
    \centering
    \begingroup
    \scriptsize
    \caption{Timber--concrete composite class validation of time-dependent effective bending stiffness values. \label{tab:TCCVer}}
    \setlength{\tabcolsep}{3.5pt}
    \renewcommand{\arraystretch}{1.2}
    \begin{tabularx}{\textwidth}{@{}>{\raggedright\arraybackslash}X|>{\centering\arraybackslash}X|>{\centering\arraybackslash}X|>{\centering\arraybackslash}X|>{\centering\arraybackslash}X|>{\centering\arraybackslash}X|>{\centering\arraybackslash}X@{}}
    \hline
    \textbf{Case} &
    \textbf{Time} &
    \(\boldsymbol{\gamma_c}\) &
    \(\boldsymbol{a_c}\) [m] &
    \(\boldsymbol{a_w}\) [m] &
    \(\boldsymbol{EI_{\mathrm{eff,manual}}}\) [$\mathrm{kN\,m^2/m}$] &
    \(\boldsymbol{EI_{\mathrm{eff,test \ file}}}\) [$\mathrm{kN\,m^2/m}$] \\
    \hline
    SLS & \(t=0\)       & 0.0798 & 0.1299 & 0.0508 & 9225.00 & 9238.66 \\
    SLS & \(t=\infty\) & 0.2279 & 0.1459 & 0.0348 & 4053.00 & 4059.49 \\
    ULS & \(t=0\)       & 0.0547 & 0.1425 & 0.0382 & 7962.00 & 7972.65 \\
    ULS & \(t=\infty\) & 0.1645 & 0.1541 & 0.0265 & 3537.00 & 3540.68 \\
    \hline
    \end{tabularx}
    \endgroup
\end{table}

\subsection{Post-tensioned concrete slab}

The implementation of the post-tensioned slab class was verified based on a representative post-tensioned concrete slab. The verification case covers the bending resistance and the quasi-permanent deflection of a two-way post-tensioned slab with distributed tendons and wall supports.

The verification model used the following set-up:\vspace{-0.8em}
\begin{itemize}
    \item Concrete C30/37: \(h_c=280\,\mathrm{mm}\)\vspace{-0.8em}
    \item Reinforcing steel B500B: \(d=14\,\mathrm{mm}\), \(s_x=s_y=150\,\mathrm{mm}\)\vspace{-0.8em}
    \item Prestressing steel Y1860: layout distributed, load-balancing $100\% \ g_{0,k}$\vspace{-0.8em}
    \item Structural system: two-way slab with wall supports and \(l_x=l_y=6.0\,\mathrm{m}\).\vspace{-0.8em}
    \item Loads: \(g_{0,k}\), \(g_{2,k}=0.75\,\mathrm{kN/m^2}\), \(q_k=2.00\,\mathrm{kN/m^2}\),  \(\varphi=2.0\).\vspace{-0.8em}
\end{itemize}

The bending resistance was calculated from the contribution of the prestressing steel and the bonded reinforcement:

\begin{equation}
\begin{aligned}
M_{Rd}
&=
\frac{P_{\infty}}{l}
\left(d_p-\frac{0.85x}{2}\right)
+
A_s f_{sd}
\left(d_s-\frac{0.85x}{2}\right)
&=
124.49\,\mathrm{kNm/m}.
\end{aligned}
\end{equation}

\begin{equation}
\begin{aligned}
\frac{P_{\infty}}{l}
&=
76.39\,\mathrm{kN/m},\\
A_s
&=
\frac{\pi \cdot 14^2}{4 \cdot 150}
=
1026\,\mathrm{mm^2/m},\\
f_{sd}
&=
435\,\mathrm{MPa},
\qquad
f_{pd}
=
1391\,\mathrm{MPa},\\
d_s
&=
h-c_{\mathrm{nom}}-\frac{d}{2}
=
280-20-\frac{14}{2}
=
253.00\,\mathrm{mm},\\
d_p
&=
h-c_{p,\mathrm{nom}}-\sqrt{\frac{A_p}{\pi}}
=
280-30-\sqrt{\frac{150}{\pi}}
=
243.09\,\mathrm{mm},\\
x
&=
\frac{\frac{P_{\infty}}{l}+A_s f_{sd}}
{b f_{cd}}
=
31.36\,\mathrm{mm},
\qquad
0.85x
=
26.65\,\mathrm{mm}.
\end{aligned}
\end{equation}

The deflection calculation, was based on the quasi-permanent load combination. The net load after load balancing and the long-term reference deflection were
\begin{equation}
\begin{aligned}
q_{\mathrm{per}}
&=
g_{0,k}+g_{1,k}+g_{2,k}+\psi_2 q_k
=
7.00+0.00+0.75+0.30\cdot2.00
=
8.35\,\mathrm{kN/m^2},\\
g_{0,k}
&=
7.00\,\mathrm{kN/m^2},\\
q_{\mathrm{net}}
&=
q_{\mathrm{per}}-g_{0,k}
=
1.35\,\mathrm{kN/m^2}.
\end{aligned}
\end{equation}

In Cubus Cedrus the respective traction load was applied onto the structural finite-element model. The analysis is documented in \ref{fig:CubusEiVal}. The uncracked elastic deflection and long-term deflection were
\begin{equation}
\begin{aligned}
q_{\mathrm{net,Cedrus}}
&=
-1.350\,\mathrm{kN/m^2},\\
w_{\mathrm{Cedrus},\varphi=0}
&=
0.0349\,\mathrm{mm},\\
w_{\varphi}
&=
w_{\varphi=0}(1+\varphi),\\
w_{\mathrm{Cedrus},\varphi=2.0}
&=
0.0349\cdot3
=
0.1047\,\mathrm{mm}.
\end{aligned}
\end{equation}


Table \ref{tab:pt_verification_methods} summarises the comparison of the manual calculations to the test file output of the verification model, showing excellent agreement, verifying the implementation of the post-tensioned concrete class.

\begin{table}[H]
    \centering
    \begingroup
    \scriptsize
    \caption{Post-tensioned concrete class validation of bending resistance and quasi-permanent deflection. \label{tab:pt_verification_methods}}
    \setlength{\tabcolsep}{3.5pt}
    \renewcommand{\arraystretch}{1.2}
    \begin{tabularx}{\textwidth}{@{}>{\raggedright\arraybackslash}X|>{\centering\arraybackslash}X|>{\centering\arraybackslash}X|>{\centering\arraybackslash}X@{}}
    \hline
    \textbf{Result} &
    \textbf{Manual} &
    \textbf{Test file output} &
    \textbf{Difference} \\
    \hline
    $M_{Rd}\ [\mathrm{kNm/m}]$ & $124.49$ & $124.49$ & $0.0\,\%$ \\
    $w_{\varphi=2.0} \ [\mathrm{mm}]$& $0.1047$ & $0.1037$ & $-1.0\,\%$ \\
    \hline
    \end{tabularx}
    \endgroup
\end{table}

\newpage
\subsection{Acoustic floor build-up}

The simplified acoustic handling was checked against Lignum example values
\cite{LignumData_Decken}. The bias is defined as
\begin{equation}
    \begin{aligned}
        \Delta R_w &= R_{w,\mathrm{calc}} - R_{w,\mathrm{Lignum}},\\
        \Delta L_{n,w} &= L_{n,w,\mathrm{calc}} - L_{n,w,\mathrm{Lignum}}
    \end{aligned}
\end{equation}


\begin{table*}[h]
    \centering
    \begingroup
    \scriptsize
    \caption{Validation of the acoustic handling against Lignum example values. \label{tab:acoustic_validation_lignum}}
    \setlength{\tabcolsep}{3.5pt}
    \renewcommand{\arraystretch}{1.2}
    \begin{tabularx}{\textwidth}{@{}l|c|>{\raggedright\arraybackslash}X|r|r|r|r|r|r|l@{}}
    \hline
    \textbf{Name} &
    \textbf{Lignum} &
    \textbf{Reference build-up} &
    \textbf{$R_{w,\mathrm{calc}}$} &
    \textbf{$R_{w,\mathrm{ref}}$} &
    \textbf{$\Delta R_w$} &
    \textbf{$L_{n,w,\mathrm{calc}}$} &
    \textbf{$L_{n,w,\mathrm{ref}}$} &
    \textbf{$\Delta L_{n,w}$} &
    \textbf{Assessment} \\
    & Bauteil & & dB & dB & dB & dB & dB & dB & \\ \hline

    Hohlkastendecke &
    A0438 &
    Hollow-core floor; cement screed $80\,\mathrm{mm}$; glass wool $s'\leq6\,\mathrm{MN/m^3}$, $30\,\mathrm{mm}$; split ballast $60\,\mathrm{mm}$; hollow-core damping &
    67.24 & 69.00 & -1.76 & 46.89 & 46.00 & +0.89 & conservative \\

    Rippendecke &
    A0231 &
    Rib/beam floor; cement screed $80\,\mathrm{mm}$; glass wool $s'\leq6\,\mathrm{MN/m^3}$, $30\,\mathrm{mm}$; split ballast $60\,\mathrm{mm}$ &
    63.81 & 67.00 & -3.19 & 49.40 & 48.00 & +1.40 & conservative \\

    CLT &
    A1088 &
    Solid timber floor; cement screed $80\,\mathrm{mm}$; glass wool $s'\leq6\,\mathrm{MN/m^3}$, $30\,\mathrm{mm}$; split ballast $60\,\mathrm{mm}$ &
    65.34 & 70.00 & -4.66 & 46.25 & 45.00 & +1.25 & conservative \\

    BSH &
    A1090 &
    Solid timber floor; anhydrite flowing screed $55\,\mathrm{mm}$; glass wool $s'\leq6\,\mathrm{MN/m^3}$, $30\,\mathrm{mm}$; split ballast $60\,\mathrm{mm}$ &
    64.23 & 67.00 & -2.77 & 49.25 & 48.00 & +1.25 & conservative \\

    HBV--BSH &
    A1347 &
    Timber--concrete composite floor; cement screed $80\,\mathrm{mm}$; glass wool $s'\leq6\,\mathrm{MN/m^3}$, $30\,\mathrm{mm}$; additional glass wool $s'\leq9\,\mathrm{MN/m^3}$, $30\,\mathrm{mm}$; concrete topping $120\,\mathrm{mm}$; timber layer $120\,\mathrm{mm}$ &
    71.47 & 72.00 & -0.53 & 45.20 & 43.00 & +2.20 & conservative \\

    \hline
    \end{tabularx}
    \endgroup
\end{table*}

\newpage
\section{Comparison model set-up}

As the scope of this work is limited to residential and office buildings, the comparison was divided into a residential case and an office case. Both cases used distinct load levels, discrete span values, structural systems, and cross-sections based on the literature review. Each slab system was assigned the most structurally efficient static system available. The complete comparison set-up is summarised in Table~\ref{tab:final_comparison_cases}. The detailed treatment of the individual cross-sections, including the fixed parameters and those optimised, is illustrated in Figures~\ref{Fig: compA} and~\ref{Fig: compB}. The optimisation was performed within predefined bounds. These bounds were selected to reflect commercially available timber widths \cite{BestWoodSchneider_Holz_2026}. In addition, they ensure compliance with fire-safety requirements for the hollow-core timber slab \cite{Lignum3.1:2015} and with minimum concrete thicknesses for cross-sections incorporating concrete \cite{SIA262_2025}. The slab and floor systems were compared according to the sustainability assessment criteria listed in Table~\ref{Tab: SustFact}. All systems must fulfil the requirements listed in Table~\ref{Tab: ReqSS}. The acoustic requirement was set to the normal requirement level.


% Morkoetter2021_HBVDecken Zitieren für abstand Kerven, Abstand rippen
% Wuerth2021_HolzBetonVerbund Zitieren für abstand schrauben
% Schaenzlin_HBVMusterdetails Zitieren für abstand Kerven, Abstand rippen, Beinhaltet kostenvergleich
\begin{table*}[h]
    \centering
    \begingroup
    \scriptsize
    \caption{Input definition for the final comparison of slab systems. \label{tab:final_comparison_cases}}
    \setlength{\tabcolsep}{3.5pt}
    \renewcommand{\arraystretch}{1.2}
    \begin{tabularx}{\textwidth}{@{}l|c|l|>{\raggedright\arraybackslash}X|>{\raggedright\arraybackslash}X|>{\raggedright\arraybackslash}X@{}}
    \hline
    \textbf{Case} &
    \textbf{$q_k$} &
    \textbf{Span} &
    \textbf{Cross-section} &
    \textbf{Description} &
    \textbf{Structural system} \\
    & kN/m$^2$ & m & & & \\ \hline
    Residential & 2.0 & 3, 5, 6, 7, 8, 10 &
    Rectangular concrete* &
    conventionally reinforced &
    2-way, full continuity, walls \\

    Residential & 2.0 & 3, 5, 6, 7, 8, 10 &
    Post-tensioned concrete &
    distributed tendon layout &
    2-way, full continuity, walls \\

    Residential & 2.0 & 3, 5, 6, 7, 8, 10 &
    Rectangular timber* &
    glue-laminated timber &
    Simple span \\

    Residential & 2.0 & 3, 5, 6, 7, 8, 10 &
    TCC flat, kerve &
    glue-laminated timber; connector: 20\,mm kerve (30\,mm for $L_0>7$\,m) \cite{Morkoetter2021_HBVDecken}&
    Simple span \\

    Residential & 2.0 & 3, 5, 6, 7, 8, 10 &
    TCC ribs, screws &
    $s_{eq}=0.06$\,m (2 rows at $s=0.12$\,m); connector: screws, $d=8$\,mm \cite{Morkoetter2021_HBVDecken, Wuerth2021_HolzBetonVerbund}&
    Simple span \\

    Residential & 2.0 & 3, 5, 6, 7, 8, 10 &
    Ribbed timber hollow core* &
    hollow-core insulation &
    Simple span \\

    Office & 3.0 & 8, 10, 12, 16 &
    Rectangular concrete* &
    conventionally reinforced &
    2-way, full continuity, columns \\

    Office & 3.0 & 8, 10, 12, 16 &
    Post-tensioned concrete &
    distributed tendon layout &
    2-way, full continuity, columns \\

    Office & 3.0 & 8, 10, 12, 16 &
    Post-tensioned concrete &
    banded tendon layout &
    2-way, full continuity, columns \\

    Office & 3.0 & 8, 10, 12, 16 &
    Ribbed concrete* &
    conventionally reinforced &
    Continuous beam \\

    \hline
    \end{tabularx}
    \endgroup
    
\end{table*}


\begin{figure}[H]
    \centering
    
    \includegraphics[width=1\textwidth]{IMAGES/cross_section_catalog_rc_rec.png}\vspace{-0.6cm}
    \includegraphics[width=1\textwidth]{IMAGES/cross_section_catalog_pc_rec_dist.png}\vspace{-0.6cm}
    \includegraphics[width=1\textwidth]{IMAGES/cross_section_catalog_pc_rec_band.png}\vspace{-0.6cm}
    \includegraphics[width=1\textwidth]{IMAGES/cross_section_catalog_rc_rib.png}\vspace{-0.6cm}

    \caption{Compared concrete cross-sections. \label{Fig: compA}}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{IMAGES/cross_section_catalog_wd_rec.png}\vspace{-0.6cm}
    \includegraphics[width=1\textwidth]{IMAGES/cross_section_catalog_wd_rib.png}\vspace{-0.6cm}
    \includegraphics[width=1\textwidth]{IMAGES/cross_section_catalog_tcc_flat.png}\vspace{-0.6cm}
    \includegraphics[width=1\textwidth]{IMAGES/cross_section_catalog_tcc_ribs.png}\vspace{-0.6cm}
    \caption{Compared timber and timber--concrete composite cross-sections. \label{Fig: compB}}
\end{figure}



\subsection{Definition of plotting envelopes}
In the final comparison, product variation was generated by selecting the best and worst available EPDs by GWP within each material class. For example, concrete strength class C30/37 was sampled using its lowest and highest available product-specific GWP values. This procedure was applied to all possible material combinations within the cross-section. The resulting optimised valid candidates defined the envelope boundaries. In addition, a statistical median was constructed from these valid candidates. This median does not necessarily correspond to an existing product-specific candidate. Based on the best valid candidate, a cross-section plot was created for better interpretability of the calculation model. 

Due to this sampling technique, the floor system height and mass also vary because of product-specific densities and varying material classes within one structural cross-section. The plotting envelopes for cost and construction time were based on triangular distributions with an accuracy range of \(\pm 20\,\%\).

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\textwidth]{IMAGES/cross_section_catalog_envelope_construction.png}
    \caption{Schematic diagram of the plotting envelope definition.}
    \label{fig:cross_section_catalog_envelope_construction}
\end{figure}


\begin{comment}
\subsubsection{Monte carlo analysis}
The plotting envelopes indicate the range of feasible optimised results but provide no information about the likelihood that a particular floor system performs best at a given span. A Monte Carlo analysis was therefore conducted to estimate the probability that each floor system achieves the lowest realised value for each sustainability assessment criterion listed in Table~\ref{Tab: SustFact

All feasible candidate solutions were assigned equal probability. In each Monte Carlo draw, one feasible candidate was sampled independently for every floor system. The corresponding assessment criterion was then re-evaluated by sampling its underlying parameters from the respective distributions. GWP and material density were sampled from their empirical data distributions, whereas cost and construction time were sampled from the assumed triangular distributions. This procedure was repeated $N = 5{,}000$ times for each span. In every draw, the floor system with the lowest realised criterion value was identified as the winning system.

The probability that system $s$ provides the best result (e.g. lowest GWP) at span $L$ was estimated as

\begin{equation}
    \widehat{P}_{s}(L)
    =
    \frac{1}{N}
    \sum_{i=1}^{N}
    \mathbf{1}_{A_{s,i}(L)},
    \label{eq:monte_carlo_probability}
\end{equation}

where $\widehat{P}_{s}(L)$ is the estimated probability that floor system $s$ performs best at span $L$, $N$ is the total number of Monte Carlo draws, and $i$ denotes the individual draw, with $i=1,\ldots,N$. Here, $A_{s,i}(L)$ denotes the event that floor system $s$ performs best in draw $i$ at span $L$. The indicator $\mathbf{1}_{A_{s,i}(L)}$ equals one if this event occurs and zero otherwise.
    
\end{comment}

\newpage
\subsection{Pairwise comparison}

The plotting envelopes indicate the range of feasible optimised results but do not describe how consistently one floor system outperforms another at a given span. A pairwise comparison based on the Monte Carlo method was therefore conducted.

All feasible candidate solutions were assigned equal probability. In each Monte Carlo draw, one feasible candidate was sampled independently for every floor system. The corresponding criterion value was then re-evaluated by sampling its underlying parameters from the respective distributions. GWP and material density were sampled from their empirical data distributions, whereas cost and construction time were sampled from the assumed triangular distributions. This procedure was repeated $N=5{,}000$ times for each span. In every draw, each floor system was compared with every other feasible system. For criteria to be minimised, system $s$ was considered to outperform system $k$ when its realised criterion value was lower. Ties were divided equally between both systems. The pairwise probability and the resulting normalised pairwise score were calculated as
\begin{equation}
\begin{aligned}
    \widehat{P}_{s \prec k,j}(L)
    &=
    \frac{1}{N}
    \sum_{i=1}^{N}
    \left[
        \mathbf{1}
        \left(
            X_{s,j,i}(L)<X_{k,j,i}(L)
        \right)
        +
        \frac{1}{2}
        \mathbf{1}
        \left(
            X_{s,j,i}(L)=X_{k,j,i}(L)
        \right)
    \right],
    \\
    S_{s,j}(L)
    &=
    \frac{1}{|\mathcal{S}(L)|-1}
    \sum_{\substack{k\in\mathcal{S}(L)\\k\neq s}}
    \widehat{P}_{s \prec k,j}(L),
\end{aligned}
\label{eq:pairwise_score}
\end{equation}

where $X_{s,j,i}(L)$ is the realised value of criterion $j$ for system $s$ in draw $i$ at span $L$, and $\mathcal{S}(L)$ is the set of feasible floor systems. The indicator function $\mathbf{1}(\cdot)$ equals one when the stated condition is satisfied and zero otherwise. Furthermore, $\widehat{P}_{s \prec k,j}(L)$ is the Monte Carlo estimate of the probability that system $s$ outperforms the individual system $k$ in a pairwise comparison. Then, $S_{s,j}(L)$ is the normalised pairwise score obtained by averaging these probabilities over all other feasible systems.

The normalised pairwise score $S_{s,j}(L)$ summarises the performance of system $s$ relative to all feasible opponents. A score of one indicates that the system outperforms all compared alternatives for the considered criterion, whereas a score close to zero indicates that it is frequently outperformed. Thus, the pairwise comparison provides a ranking of the floor systems that accounts for the variability represented by the plotting envelopes. This ranking identifies not only the best performing system for each criterion but also the \mbox{relative performance of the remaining alternatives.}
